language: node_js
node_js:
  - "8.10"

addons:
  chrome: stable

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

script:
  - npm run build:prod
  - npm run test

notifications:
  email:
    on_failure: change
    on_success: change